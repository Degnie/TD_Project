namespace TD_Project.Application;

// spec: RN-11 — espejo de Domain.Shared.Trayectoria para el reporte externo de Application,
// evita que Presentation dependa de un enum de Domain.
public enum TrayectoriaResolucion
{
    A,
    B
}
