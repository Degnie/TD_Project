<delta_aprobado>
  <resumen> Exposición vía API del reporte de régimen ya calculado por `Application.ReporteRegimen.Calcular` (RN-19, CU-24): nuevo `ReporteRegimenDto` (espejo de `ReporteRegimenResultado`, con `Fases`/`RegimenOptimo`) agregado a `ResultDto`, cableado únicamente en `POST /api/strategies/dsl/run` (único endpoint con acceso simultáneo a un único `resultado.Trades` y a `velas`). Sin cambios en `ReporteRegimen.Calcular`, `ClasificadorRegimen`, ni en `POST /api/backtest/run`/`POST /api/capital-managers/recommend`. </resumen>
  <clasificacion> estructural </clasificacion>
  <ids_nuevos> ninguno — RN-19 y CU-24 ya exigen textualmente que "el reporte final consolida el PnL, Win Rate y Drawdown discriminados por cada una de las 3 fases" y que el sistema "muestra el desempeño desglosado en las 3 fases resaltando el régimen óptimo"; el cálculo (`ReporteRegimen.Calcular`) ya existe y ya cumple esa regla — este delta solo cablea un endpoint para exponerlo, sin definir comportamiento nuevo. </ids_nuevos>
  <ids_modificados> RN-19 (clarificación: el reporte segmentado ya normado se expone ahora en `POST /api/strategies/dsl/run`, no solo en el cálculo interno), CU-24 (idem, activación concreta por endpoint), RNF-16 (nuevo `ReporteRegimenDto` como dato estructurado, separado de la interpretación en `ExplicacionDto.RegimenOptimoDescripcion`, ya existente). </ids_modificados>
  <ids_retirados> ninguno </ids_retirados>
  <decision_adr> ninguno — no se introduce ninguna interfaz, adaptador ni componente arquitectónico nuevo; `ReporteRegimenDto` sigue el mismo patrón ya usado para `IncapacidadDto`/`ExposicionFinalDto` (caso14) y `ResultDto.ReporteRegimen` sigue el patrón de campo opcional trailing ya establecido. </decision_adr>
  <spec_version> 7.0 </spec_version>
</delta_aprobado>
