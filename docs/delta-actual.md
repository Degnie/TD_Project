<delta_aprobado>
  <resumen> Activación condicional del bloqueo de capacidad (RN-12/CU-15) exclusivamente para el flujo de cliente final de SPEC 7.0 (endpoints `/api/strategies/dsl/run` y `/api/capital-managers/recommend`), mediante el campo opcional `BloquearPorCapacidad: bool = false` en `ConfiguracionExperimento`. El laboratorio interno y el endpoint demo `/api/backtest/run` conservan el comportamiento histórico (D-059/D-060: solo observación, nunca bloqueo). Se extiende además `ResultDto`/`ExplicacionDto` para exponer `Incapacidades`, la exposición final de posiciones abiertas, y advertencias explicativas en lenguaje simple cuando el backtest cierra con posiciones vivas o con órdenes bloqueadas por capacidad. </resumen>
  <clasificacion> estructural </clasificacion>
  <ids_nuevos> ninguno — RN-12 y CU-15 ya especifican el rechazo atómico de órdenes por capacidad ("la orden se ajusta a la capacidad máxima permitida o se rechaza atómicamente"; "Falla validación preventiva (RN-12) → OrderRequestRejected"); este delta activa ese comportamiento ya normado, restringido al flujo de cliente final, sin invalidar la excepción histórica D-059/D-060 para el laboratorio. </ids_nuevos>
  <ids_modificados> RN-12 (clarificación: el rechazo atómico que la regla ya describe se activa condicionalmente vía `BloquearPorCapacidad`, no incondicionalmente), CU-15 (idem, condicionado al flujo de cliente final), RNF-16 (se añaden advertencias de posiciones abiertas e incapacidad de capital al reporte explicativo). </ids_modificados>
  <ids_retirados> ninguno </ids_retirados>
  <decision_adr> ninguno — no se introduce ninguna interfaz, adaptador ni componente arquitectónico nuevo; `BloquearPorCapacidad` sigue el patrón ya existente de campos opcionales de `ConfiguracionExperimento` (`Instrumento?`, `Costes?`, `Sizing?`). </decision_adr>
  <spec_version> 7.0 </spec_version>
</delta_aprobado>
