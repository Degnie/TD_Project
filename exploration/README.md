# exploration/

Material temporal para la auditoría exploratoria de uso cotidiano
(`agent-workflow/prompts/12-auditoria-roles-claude.md`, adaptado a un solo rol).

No es parte del producto: no se referencia desde `src/`, no se empaqueta, no
tiene garantías de estabilidad. Vive fuera de `Presentation.Api.Demo` porque
esa carpeta es demo de producto, no fixture de auditoría.

Se elimina (o se decide conservar explícitamente) al cerrar el ciclo de
auditoría.
